class Todo {
  String? uid;
  String? title;
  bool? isChecked;
  String? createdAt;
  String? completedAt;

  Todo(
      {this.uid, this.title, this.isChecked, this.createdAt, this.completedAt});
}
